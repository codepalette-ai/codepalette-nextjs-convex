{
	"name": "shadcn-ui-monorepo",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"build": "turbo build",
		"dev": "turbo dev",
		"format": "biome check --write",
		"update-deps": "npx npm-check-updates --deep -u && pnpm install",
		"clean": "rm -rf node_modules && rm -rf .turbo && rm -rf .next && rm -rf apps/*/.next && rm -rf apps/*/.turbo && rm -rf apps/*/node_modules"
	},
	"devDependencies": {
		"@biomejs/biome": "2.1.3",
		"@workspace/typescript-config": "workspace:*",
		"turbo": "^2.5.5",
		"typescript": "5.9.2",
		"@types/node": "24.1.0"
	},
	"packageManager": "pnpm@10.14.0",
	"engines": {
		"node": ">=22"
	},
	"dependencies": {
		"convex": "^1.25.4"
	}
}
